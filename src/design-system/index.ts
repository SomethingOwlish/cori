/**
 * Coriolis Design System — barrel export.
 *
 * Usage:
 *   import { Card, Button, StatBlock, Meter } from "../design-system";
 *
 * Global tokens + base layer are wired once in `main.tsx`
 * (`import "./design-system/tokens.css"` and `"./design-system/base.css"`).
 * See README.md in this folder for the brand rules (voice, color, type, motifs).
 */

export * from "./primitives";
